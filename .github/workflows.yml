name: nestjs-action-flows
on:
  push:
    branches:
      develop
jobs:
  build:
    name: Build application
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: 'Install dependency'
        run: |
          npm install
          npm run build